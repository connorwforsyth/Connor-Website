// "use client";
// import { useSearchParams } from "next/navigation";

// const searchParams = useSearchParams();
// const hasRef = searchParams.has("ref");
// const innerRef = hasRef ? searchParams.get("ref") : "";

// export const ref = hasRef ?? innerRef;
